import { listingRank } from 'block-share/helpers/listing-rank';
import { module, test } from 'qunit';

module('Unit | Helper | listing rank');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingRank([42]);
  assert.ok(result);
});
